services:
  # FastAPI Backend Service
  - type: web
    name: neuroscan-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PYTHON_VERSION
        value: "3.10"
      - key: PORT
        value: "8000"
    healthCheckPath: /health

